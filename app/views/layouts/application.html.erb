<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Dataplay" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Dataplay" %>">
    <%= stylesheet_link_tag "application" %>
    <%# Foundation 5 won't work with Rails Turbolinks %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "http://cdnjs.cloudflare.com/ajax/libs/Chart.js/0.2.0/Chart.js" %>
    <%= javascript_include_tag "http://code.jquery.com/jquery-1.10.2.min.js" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
      <%= render 'layouts/messages' %>
    </header>
      
    <div class="row">
    <div class="large-12">

    
    <%= yield %>

    </div>
    </div>

    <%# Foundation 5 won't work when application.js is included in HEAD %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
